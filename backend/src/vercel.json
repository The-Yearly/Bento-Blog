{
  "version": 2,
  "builds": [
    {
      "src": "cmd/main.go",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "gin-bin"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/gin-bin/main"
    }
  ]
}
